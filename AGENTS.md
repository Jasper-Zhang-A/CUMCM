# AGENTS.md

## 1. 任务与文件边界

完成 C 题四问的建模、实现、因果回测和独立核验。统一主线：**确定性调度 → 条件随机规划 → 经济阈值滚动优化 → 电价与供需联合优化**。CyclePatch 只用于周期结构和场景构造，不迁移原神经网络。

```text
CUMCM/
├── AGENTS.md
├── 题c .md                         # 题目原文，只读
├── data/
│   ├── data_clean.csv              # 唯一模型输入，只读
│   └── results_template_clean.csv  # 五项正式计算结果的唯一写入位置
└── BatteryLife/                    # 参考代码库，只读
```

- 题目文件按实际名称读取，兼容 `题c .md` 与 `题c.md`，不得擅自改名。题目决定任务和物理条件，本文件规定项目实施与写入方式。
- **全部正式结果统一填入 `data/results_template_clean.csv`。不得把 `result1.xlsx`、`result2.xlsx`、`result3.xlsx`、`result4-2.xlsx`、`result4-3.xlsx` 当作计算过程中的写入目标。** 用户明确要求最终导出时，才从核验后的 CSV 单向生成 XLSX。
- 自行创建需要的 `src/`、`configs/`、`scripts/`、`tests/`、`reports/`、`logs/`、`cache/`。中间缓存、运行轨迹与报告可存于这些目录，但不得另立一份正式答案替代上述 CSV。
- 若存在 `README_数据口径与填写说明.md`、`conversion_audit.json`，先只读核对；说明中的旧输入名 `attachments_clean.csv` 对应本项目的 `data/data_clean.csv`。
- 题面条件、用户确认口径、建模假设必须分开记录。未规定细节须在配置及 `reports/model.md` 中明确选择，不能静默补成“题目已知条件”。

## 2. 数据与因果约束

两个 CSV 按 `utf-8-sig`、逗号分隔读取。先区分 `dataset_id` / `record_type` / `time_semantics` / `time_basis`，再解析数值；禁止整表统一补零、前向填充、`dropna` 或不分类型求和。模板不是训练数据。

- 每天 144 段，`slot_index=0..143`，`Δt=1/6` 小时。`00:10` 是 `[00:00,00:10)` 的右端点，1440 分钟表示当日 24:00。功率转电量：`kWh = kW × Δt`。
- 区间输入按 `(dataset_id, day_id, slot_index)` 对齐；附件1保留 `typical_day`，不虚构日期，不转换题目时区。
- 附件3保留 `(dataset_id, issue_time, valid_time)` 全部版本。先筛选 `issue_time <= decision_time`，再选预报；不能预先保留全年的“最终最新版本”。
- 整点预报先按声明的插值/积分方法转为十分钟代表功率，再转电量；边界处理只用当时已知信息，不使用后续发布的预报或未来实测值补点。
- 训练、标准化、相似块筛选、残差估计及调参只用决策前可获得的数据。实测负荷、光伏及实时价格仅在对应运行阶段揭示；读取到文件不等于决策时已知。
- 场景决策须满足非前瞻性：相同信息下采取相同动作。逐场景提前看完整未来的调度只能作为明确标注的规划近似或理想下界，不能当成可执行的多阶段最优策略。
- 输出问题2—4的 2025-02-01 至 2025-12-31。储能按 2025-01-01 00:00 的 6000 kWh 初始化，因果预热1月并承接日末状态；无历史时的冷启动规则单独声明，禁止2月初或每天重置电量。

## 3. 共用物理模型

以母线侧每时段电量计量：`q` 为当前有效计划购电，`u` 为紧急购电，`c,d` 为充、放电量，`L,G` 为负荷与光伏电量，`E` 为电池内部储电量，`r` 为弃置富余电量。除净负荷外，上述电量非负。

\[
q_t+u_t+G_t+d_t=L_t+c_t+r_t,\qquad
E_{t+1}=E_t+\eta_c c_t-d_t/\eta_d.
\]
\[
1200\le E_t\le10800,\qquad
0\le c_t\le(5000\Delta t)z_t,\quad
0\le d_t\le(5000\Delta t)(1-z_t),\quad z_t\in\{0,1\}.
\]

题面额定容量为 12000 kWh，运行范围以上述上下限为准。题面效率为90%；单向/往返效率的解释、功率限制的计量侧、是否允许无代价弃余，须显式记录并保持全项目一致。不得重复扣除效率，不引入未声明的售电收益。

**默认采用 MILP 保证充放电互斥，不能直接执行同时充放电的 LP 原始解。** 仅在允许无代价弃余、且当前其他约束均支持时，证明 LP 等价后才能使用 LP。可用以下构造消除同充同放：

\[
\epsilon=\min\{c_t,d_t/(\eta_c\eta_d)\},\quad
c'_t=c_t-\epsilon,\quad d'_t=d_t-\eta_c\eta_d\epsilon.
\]

该构造保持储能增量不变，使净供电增加 `(1−η_cη_d)ε`，至少一个充放电量归零。必须核验弃余及全部约束仍可行；也可在最低费用固定后二次最小化总充放电量。证明不适用或修正未通过核验时，保留 MILP。

## 4. 四问实施与核心公式

### 问题一：确定性调度

使用 `A1_PRICE`、`A1_LOAD`、`A1_PV_FORECAST`，令 `u=0`，最小化
\[
\min\sum_t p_tq_t,\qquad E_{144}=E_0.
\]

明确采用的初始储电量；不要把 `typical_day` 当作真实日历日。以 MILP 为基准验证 LP 等价性，不强行加入 CyclePatch。

### 问题二：条件场景与成本导向采购

使用 `A1_PRICE` 和 `A2_LOAD/A2_PV_ACTUAL` 的历史数据。零点预测净负荷 `n=L−G`，利用第5节生成场景及权重，求解
\[
\min\sum_t p_tq_t+\sum_j\omega_j\sum_t5p_tu_t^{(j)}.
\]

零点购电计划在所有场景中相同，当天不再修改；储能与紧急购电通过因果执行器纠偏。预测与风险参数按历史滚动验证中的实际费用选择，不能只按 MAE/MSE 选择。

解释与单元测试基准：单时段、无储能、无剩余价值、`p>0`、外购需求 `N≥0` 时，
\[
\min_{q\ge0}\{pq+5p\,\mathbb E[(N-q)^+]\}
\quad\Rightarrow\quad q^*=Q_{0.8}(N).
\]

80%来自购少/购多的非对称费用，**不是完整储能模型的逐时固定采购规则**。

### 问题三：新信息与经济不调整区间

在问题二框架上使用当时已发布的 `A3_PV_FORECAST`。零点制定计划；6、12、18点根据新预报、已发生偏差及实际储电量，更新场景并优化当日尚未开始的区间，已执行决策冻结。允许保持原计划。

**用户确认口径：调减部分只付50%违约费用，不是原计划全额支付后再额外罚50%。** 对同一单价 `p`、一次调整，原计划 `q_0` 调至 `q` 的计划及调整合计费用为
\[
K(q;q_0)=p\bigl(q+\tfrac12|q-q_0|\bigr).
\]

例如 `p=1,q_0=100,q=80` 时合计90元；不得重复加上原计划100元。多次修订的结算参考版本、增减差额与价格引用必须另行明确，建立统一账本；**不能逐次累加完整的 K 或多个完整计划版本**。

在上述单时段简化条件、仅有一次调整机会时，加入紧急购电费后：
\[
q^*=\min\{Q_{0.9},\max(q_0,Q_{0.7})\}.
\]

即原计划位于更新后需求分布的70%—90%分位之间时不调整。对同价、凸的剩余成本模型，若 `B_t` 表示额外一度计划电量减少的预期后续费用，则局部不调整区间为 `0.5p_t ≤ B_t ≤ 1.5p_t`。完整储能、多次调整或不同结算机制须重新计算边际价值，不能硬套分位数阈值。

比较“仅零点预报”“增加预报但不修改购电”“按经济价值选择修订”。新信息可被忽略时，理论最优期望费用不会上升；回测变差须分析预测、调整、近似或样本因素，不直接归因于信息有害。

### 问题四：电价与供需联合风险

保持问题二、三各自的信息结构，将价格替换为 `A4_PRICE` 的预测/联合场景，分别输出 `result4-2`、`result4-3`。不得提前使用未来实际电价；价格何时可知、计划及修订按何价格结算必须声明，实际费用按所选一致口径复算。

同一历史片段内配对负荷、光伏及价格误差，保留时间与变量关联。场景概率仍为 `ω_j`，价格已进入费用函数时，不再无依据地给概率重复乘价格。

单时段、无储能、正价格，且计划和紧急电量分别按同一随机实际价格 `p` 及其5倍结算时，解释基准为
\[
F^{(p)}(q)=\frac{\mathbb E[p\mathbf1_{\{N\le q\}}]}{\mathbb E[p]},
\qquad q^*=Q^{(p)}_{0.8}.
\]

其他结算机制不得沿用该加权80%结论。

## 5. CyclePatch：只迁移周期结构

先阅读 `BatteryLife/` 中 CyclePatch 的周期内/周期间组织方式，记录借鉴代码位置。无需训练原模型、迁移权重、加载其电池数据或安装整套训练环境。

主流程：**日—周分层对齐 → 完整联合误差路径 → 当前信息条件赋权 → 原 LP/MILP/MPC**。

- 用 `X_w[d,s,m]` 组织周、星期、十分钟时段及变量；周是外层组织，日内144段不压缩。分别验证负荷、光伏、电价的周期性，不强制所有变量采用七天周期，不预设低负荷必为周末。
- 先建立昨日同期、上周同期及含星期特征的简单预测基线。使用历史滚动预测残差，不能拿全年度拟合残差代替真实预测误差。
- 从决策前已完整揭示的历史块提取整日/剩余预测范围的联合误差路径，按星期相位、发布时刻和提前量对齐，不将各时段、各变量独立打乱。保留连续缺口、跨时段富余与高价缺电情景。
- 以当前和历史各自决策时可见的特征 `z_τ,z_j` 计算标准化距离；不能用本周尚未发生的整周统计量找相似周。样本少时放宽条件、共享日级样本，不能把重叠块当作独立新增周样本。

\[
\boldsymbol n_\tau^{(j)}=\widehat{\boldsymbol n}_\tau+
\boldsymbol\varepsilon_n^{(j)},\qquad
\omega_j(\tau)=\frac{e^{-D(z_\tau,z_j)/h}}{\sum_k e^{-D(z_\tau,z_k)/h}},\quad h>0.
\]

问题四同步构造 `p^(j)=p_hat+ε_p^(j)`，保持同一历史片段的配对关系。场景权重非负且和为1；原始负荷、光伏及价格的物理范围须核验。净负荷允许为负，不能把光伏富余截掉。

可选的储能导向场景压缩特征：对单位为kWh的净负荷误差路径，使用
\[
D_E(i,j)=\max_{1\le k\le H}\left|\sum_{t=1}^{k}
(\varepsilon_t^{(i)}-\varepsilon_t^{(j)})\right|.
\]

它帮助保留累计缺口不同的场景，不替代真实储能约束。日内新信息先更新场景及权重，再决定是否付费调整。不得照搬电池曲线的时间伸缩、随意平移峰值、按周重置储能或改成每周一次决策。

## 6. 储能机会价值

不要把“出现缺口就优先放完电”作为不可更改规则。固定当前信息、已有计划及其他状态，设 `V_τ(E)` 为预期后续费用，可通过储电量网格重复优化得到
\[
\lambda_\tau(E)\approx
\frac{V_\tau(E)-V_\tau(E+\Delta E)}{\Delta E}.
\]

母线侧放出一度电的机会成本约为 `λ/η_d`，应与当前避免的购电费用比较。采用周期条件场景估计价值；可用分段线性期末价值或延长预测范围减轻日末短视，无需强化学习。问题二—四延续实际储电量，不强制每天首尾相同；期末价值只是决策辅助项，不计入实际电费。

## 7. 结果模板写入合同

只按现有记录键定位并填写 `data/results_template_clean.csv`，保留原列、已有行顺序、`record_id`、时间轴、单位及来源元数据。不得重建、删除空值行或改动模板结构来迁就算法；除 `value/status` 和待填写的紧急事件字段外，不改已有内容。按模板规则追加实际紧急事件是唯一必要的行扩展。

- `result_id` 保留 `result1/result2/result3/result4-2/result4-3`。问题1为 `typical_day`，其他问题按现有334天记录填写。
- `initial_00` 保存原零点计划；`update_06/12/18` 保存该次**修订后的时段总计划量**，不是差额或额外购电。按最新已采用版本覆盖未来时段重建有效计划，不把版本相加。
- 未采用的修订填 `status=not_used,value` 留空；采用但数量未变化时填上一有效计划量，不能以0表示“未变化”。
- `storage_flow` 分别累计六个四小时区间的充电量和放电量；不能用净储电变化替代。`storage_state` 填真实0点和24点储电量。
- 连续紧急购电时段合并为事件，跨日拆分；有多个事件时递增 `event_index` 并生成唯一 `record_id`。无事件保留一行 `status=no_event`，区间及数值留空；不能受模板空行数限制。
- `planned_daily_summary` 按初始计划指标汇总；`adjusted_daily_summary` 的结算范围在 `reports/model.md` 中定义后填写。不得把紧急购电费静默混入初始计划费用。模板未容纳的诊断和费用分解写入报告，不新增指标列。
- 完成并核验的记录标为 `filled`；真实零写0，未算出或失败的记录保持 `unfilled`，不能伪装为零或无事件。写入前备份，独立验证后原子替换，保持UTF-8 BOM与原字段结构。

## 8. 实施顺序与验收

按“数据审计 → 单日物理模型 → 跨日小样本 → 四问基线 → 周期场景/机会价值/阈值改进 → 全时段回测 → 核验写入”执行，不能只交建模说明而不实现。建立统一运行入口，例如 `python scripts/run_all.py`；参数、随机种子、依赖与求解器版本、求解状态及误差容限均记录。

独立核验器不得仅复用求解器目标值，须从逐时执行轨迹重算：供需平衡、储能递推与上下限、充放电功率及互斥、跨日连续性、信息时间戳、版本覆盖、各项费用和事件汇总。单测覆盖：调减100→80的费用、采用但不变的修订、无事件/多事件、跨日边界、LP互斥修正；不通过不得发布为有效结果。

消融实验保持基准预测、信息集、结算及初始状态可比，分别检验：独立误差与整段误差、普通日块与星期对齐、静态权重与条件更新、缺口优先放电与机会价值、零点计划与经济阈值修订、价格独立抽样与联合抽样。调参只用历史验证，最终比较实际总费用，同时报告紧急购电、调整费用、弃余电量、期末库存与运行时间，不能把不同末端库存直接算作节费收益。

交付可复现代码、已核验的统一CSV，以及 `reports/model.md`（公式、假设、借鉴来源）、`reports/validation.md`（核验结果与未完成项）、`reports/comparison.md`（基线、消融及题目指定日期表格）。所有数值结论来自本次可复现计算，不复制对话中的提升比例，不预设创新方法一定优于基线。

- **Python environment:** use `/home/jasper/miniconda3/envs/tslib/bin/python` for this project.
